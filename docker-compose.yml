services:
  gemini:
    user: "1000:1000"
    image: gemini:LATEST
    stdin_open: true
    tty: true
    container_name: gemini
    build:
      context: .
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./workspace:/workspace
